# 📋 Инструкция по установке Audit Processor

## Архитектура решения
1. **OCR для фото** → EasyOCR (работает без интернета)
2. **Парсинг документов** → python-docx, PyMuPDF
3. **Локальная нейросеть** → Ollama с моделью llama3.2 или qwen2.5 (русский язык)
4. **Excel** → openpyxl
5. **GUI** → tkinter (встроен в Python)

---

## 🚀 Пошаговая установка

### ШАГ 1: Распаковать архив
- Распакуйте `audit_processor.zip` в любую папку
- Например: `C:\audit_processor\`

---

### ШАГ 2: Установить Python 3.10+

**Windows:**
1. Скачайте: https://www.python.org/downloads/
2. **ВАЖНО:** При установке поставьте галочку ✅ **"Add Python to PATH"**
3. Установите
4. Проверьте установку:
   ```cmd
   python --version
   ```
   Должно показать: `Python 3.10.x` или выше

---

### ШАГ 3: Установить зависимости Python

Откройте командную строку в папке с программой:
```cmd
cd C:\audit_processor
pip install -r requirements.txt
```

Если файл `requirements.txt` отсутствует, установите вручную:
```cmd
pip install easyocr python-docx PyMuPDF openpyxl requests
```

---

### ШАГ 4: Установить Ollama (локальная нейросеть)

#### 4.1. Скачать и установить Ollama
**Windows:**
1. Скачайте: https://ollama.com/download/windows
2. Запустите установщик
3. Дождитесь завершения установки

#### 4.2. **ВАЖНО: Перезапустите командную строку**
После установки Ollama **обязательно закройте и откройте новую** командную строку (cmd), чтобы PATH обновился.

#### 4.3. Проверьте, что Ollama работает
```cmd
ollama list
```
Если команда работает без ошибок - установка прошла успешно!

#### 4.4. Скачать модель для русского языка
```cmd
ollama pull qwen2.5:latest
```
- Размер модели: ~5 ГБ
- Время загрузки: 5-10 минут (зависит от интернета)
- Прогресс отображается в консоли

**Альтернатива (если нужна меньшая модель):**
```cmd
ollama pull llama3.2:latest
```

#### 4.5. Проверьте, что модель установлена
```cmd
ollama list
```
В списке должна появиться модель `qwen2.5:latest`

---

### ШАГ 5: Запуск программы

#### Способ 1: Использовать bat-файл (рекомендуется)
1. Скопируйте файл `start_audit.bat` в папку с программой
2. Дважды кликните на `start_audit.bat`
3. Программа автоматически:
   - Проверит, запущен ли Ollama
   - Запустит Ollama, если нужно
   - Проверит наличие модели
   - Запустит приложение

#### Способ 2: Запуск вручную
```cmd
cd C:\audit_processor
python audit_processor.py
```

---

## 🔧 Решение проблем

### Проблема: "ollama не является внутренней или внешней командой"

**Причина:** PATH не обновился после установки Ollama

**Решение:**
1. **Самое простое:** Закройте cmd и откройте заново
2. Попробуйте команду снова: `ollama list`
3. Если не помогло - запустите Ollama по полному пути:
   ```cmd
   "C:\Program Files\Ollama\ollama.exe" pull qwen2.5:latest
   ```
4. Если и это не работает - добавьте PATH вручную:
   - Win+R → `sysdm.cpl`
   - "Дополнительно" → "Переменные среды"
   - В "Системные переменные" найдите `Path`
   - "Изменить" → "Создать"
   - Добавьте: `C:\Program Files\Ollama`
   - Нажмите ОК везде
   - **Перезапустите cmd**

---

### Проблема: "Error: listen tcp 127.0.0.1:11434: bind"

**Причина:** Ollama уже запущен в фоновом режиме

**Решение:** Это нормально! Ollama работает. Просто запустите программу:
```cmd
python audit_processor.py
```

---

### Проблема: "Error: pull model manifest: Get ... EOF"

**Причина:** Проблема с сетью или реестром Ollama при загрузке модели

**Решение:**

1. **Проверьте интернет-соединение**
2. **Попробуйте другую модель:**
   ```cmd
   ollama pull llama3.2:latest
   ```
   или
   ```cmd
   ollama pull mistral:latest
   ```
3. **Подождите и попробуйте снова** (иногда реестр перегружен)
4. **Используйте прокси** (если в корпоративной сети):
   ```cmd
   set HTTP_PROXY=http://proxy:port
   set HTTPS_PROXY=http://proxy:port
   ollama pull qwen2.5:latest
   ```
5. **Приложение работает БЕЗ модели в демо-режиме** (без AI-анализа)

---

### Проблема: Кракозябры в .bat файле (кириллица не отображается)

**Причина:** Неправильная кодировка файла

**Решение:**
1. Откройте .bat файл в Notepad++
2. Меню: Кодировка → Преобразовать в Windows-1251
3. Сохраните
4. ИЛИ используйте файл `start_audit.bat` из этого репозитория

---

### Проблема: "python не является внутренней или внешней командой"

**Причина:** Python не установлен или не добавлен в PATH

**Решение:**
1. Переустановите Python с галочкой ✅ "Add Python to PATH"
2. Или добавьте Python в PATH вручную (аналогично Ollama)

---

### Проблема: "can't open file ... audit_processor.py: No such file"

**Причина:** Файл audit_processor.py отсутствует в текущей папке

**Решение:**
1. **Убедитесь, что вы в правильной папке:**
   ```cmd
   dir audit_processor.py
   ```
   Должен показать файл. Если нет - перейдите в правильную папку:
   ```cmd
   cd C:\путь\к\audit_processor
   ```
2. **Проверьте содержимое архива** - файл должен быть распакован
3. **Скачайте файлы из репозитория:**
   - `audit_processor.py`
   - `requirements.txt`
   - `start_audit.bat`

---

## ✅ Проверка установки

Выполните все команды по порядку:

```cmd
# 1. Проверка Python
python --version

# 2. Проверка pip
pip --version

# 3. Проверка Ollama
ollama list

# 4. Проверка модели
ollama list | findstr qwen2.5
```

Если все команды работают без ошибок - установка завершена успешно! ✅

---

## 📂 Структура проекта

```
audit_processor/
├── audit_processor.py      # Главный файл программы
├── requirements.txt         # Зависимости Python
├── start_audit.bat          # Скрипт запуска (используйте этот!)
├── README.md                # Документация
└── data/                    # Папка с данными (создается автоматически)
```

---

## 🎯 Быстрый старт (для опытных пользователей)

```cmd
# Установка всего за 5 команд:
cd C:\audit_processor
pip install easyocr python-docx PyMuPDF openpyxl requests
ollama pull qwen2.5:latest
python audit_processor.py
```

---

## 💡 Полезные команды Ollama

```cmd
# Показать все установленные модели
ollama list

# Удалить модель (если нужно освободить место)
ollama rm qwen2.5:latest

# Остановить Ollama
taskkill /F /IM ollama.exe

# Проверить, работает ли Ollama
curl http://localhost:11434
```

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте раздел "Решение проблем" выше
2. Убедитесь, что все команды из раздела "Проверка установки" работают
3. Проверьте логи программы
4. Создайте issue в репозитории с описанием ошибки

---

**Версия инструкции:** 2.0
**Дата обновления:** Ноябрь 2025
**Автор:** dburlaku
